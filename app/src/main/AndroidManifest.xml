<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools"
          package="com.example.myclinic"
>

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>


    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"

    >

        <activity android:name=".welcome">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".BookDetails">
        </activity>
        <activity android:name=".Reverstion">
        </activity>
        <activity android:name=".Reverse">
        </activity>
        <activity android:name=".Homeuser">
        </activity>
        <activity
                android:name=".patient"
                android:label="@string/title_activity_patient"
                android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity
                android:name=".mydoctor"
                android:label="@string/title_activity_mydoctor"
                android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity android:name=".signup">
        </activity>
        <activity android:name=".signin">

        </activity>
    </application>

</manifest>